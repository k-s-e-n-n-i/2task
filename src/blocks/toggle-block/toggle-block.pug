mixin toggleBlock(obj)
  .toggle-block
    if obj.active == "off"
      .toggle-block__toggle.toggle-block__toggle_off
        .toggle-block__item
    if obj.active == "on"
      .toggle-block__toggle.toggle-block__toggle_on
        .toggle-block__item
    p= obj.text