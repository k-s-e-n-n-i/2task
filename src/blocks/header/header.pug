include ~@Blocks/btn/btn.pug

mixin header(obj,...items)
  header.header
    div.header__content-container
      a.header__logo-link(href="./landing-page.html")
        img.header__logo(src=require("@Blocks/header/logo.svg").default alt="logo")
      div.header__menu-mobile
      div.header__links.header__links_hidden
        nav.header__block-menu
          ul.header__menu
            each item in items
              if item.state == "active"
                - var active = "header__menu-li_active";
              else
                - var active = "";
              if item.type == "expand"
                - var expand = "header__menu-li_expand";
              else
                - var expand = "";
              li(class="header__menu-li " +active+" "+expand)
                a.header__menu-a(href= item.link)= item.menuItem
                if item.submenu != false
                  ul.header__submenu
                    each sbm in item.submenu
                      li.header__submenu-li
                        a.header__submenu-a(href= sbm.link)= sbm.menuItem
        div.header__block-login
          if obj.authorization == false
            +btn(
              {
                text : "Войти",
                link : "./sign-in.html",
                arrow : false,
                border: true,
                hover: false
              }
            )
            +btn(
              {
                text : "Зарегистрироваться",
                link : "./registration.html",
                arrow : false,
                border: false,
                hover: false
              }
            )
          else
            div.header__block-login-hr
            p= obj.name